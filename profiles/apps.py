from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """
    Configuration de l'application Django pour 'profiles'.

    Cette classe configure l'application 'profiles', spécifiant son nom
    pour une intégration optimale avec le framework Django. Elle sert à
    reconnaître 'profiles' comme une application à part entière du projet.
    """
    name = 'profiles'
