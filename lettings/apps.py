from django.apps import AppConfig


class LettingsConfig(AppConfig):
    """Configuration de l'application Lettings de Django.

    Cette classe configure l'application Lettings. Elle est utilisée par Django pour
    initialiser l'application avec ses paramètres par défaut ou personnalisés.
    """
    name = 'lettings'
